# 멱등성 — 모든 층에 공통인 네 가지

`develop_rule` 스킬의 참조 문서. 셋업·코드·API 어느 층에서도 같은 순서로 밟는다.

---

## 모든 층에 공통인 네 가지

### 1. 무엇이 "같은 것"인지 먼저 정의한다

멱등성은 "이 두 작업은 같은 작업이다"라는 판단 위에 서 있다. 그 판단 기준(자연 키, 비즈니스 키, 요청 ID)이 없으면 멱등성도 없다. 코드를 쓰기 전에 이걸 먼저 정해라.

후보가 여럿이면 **재실행해도 변하지 않는 값**을 고른다 — 파일명, 외부 시스템이 준 ID, 사람이 정한 코드. 자동 증가 ID나 생성 시각은 후보가 아니다. 매 실행마다 새로 생기므로 "같음"을 판정할 수 없다.

고른 근거는 스키마에 주석으로 남기고 최종 보고에 명시한다. 데이터가 쌓인 뒤에는 바꾸기 어렵다.

### 2. 상태를 "만들지" 말고 "맞춘다"

명령형("만들어라")이 아니라 선언형("이 상태가 되게 하라")으로 표현하면 멱등성이 거의 공짜로 따라온다.

```python
# 두 번째 실행에서 터진다
os.mkdir(path)
db.execute("CREATE TABLE users (...)")
users.append(new_user)

# 몇 번을 실행해도 같은 상태
os.makedirs(path, exist_ok=True)
db.execute("CREATE TABLE IF NOT EXISTS users (...)")
users_by_id[new_user.id] = new_user
```

함수 이름도 이 사고를 따라간다. `create_bucket()`보다 `ensure_bucket()`이 낫다. 이름이 계약을 드러내면 호출하는 쪽이 재시도해도 되는지 매번 고민하지 않는다.

### 3. 어디서 죽어도 재실행이 답이 되게 한다

작업이 5단계라면, 3단계에서 프로세스가 죽는 경우를 실제로 상상해라. 그 상태에서 처음부터 다시 실행하면 어떻게 되는가?

- **깨끗하게 완료된다** → 좋다.
- **이미 있다고 에러난다** → 2번 원칙을 적용해라.
- **중복이 생긴다** → 1번 원칙으로 돌아가라. "같음"의 기준이 없다.
- **모르겠다** → 이게 가장 위험하다. 알 수 있게 만들어라.

여러 단계가 전부 성공하거나 전부 실패해야 한다면 트랜잭션으로 묶어라. 트랜잭션으로 못 묶는 경계(외부 API 호출 등)가 있으면, 그 경계를 코드에서 눈에 띄게 표시하고 각 단계를 개별적으로 멱등하게 만들어라.

### 4. 확인과 변경 사이의 틈을 없앤다

"있는지 보고, 없으면 만든다"는 단일 스레드에서만 안전하다. 동시 실행이 있으면 두 프로세스가 동시에 "없다"를 보고 둘 다 만든다.

```python
# 경합에 취약하다 — 두 요청이 동시에 통과한다
if not db.query(User).filter_by(email=email).first():
    db.add(User(email=email))

# 확인과 삽입이 한 연산이다
db.execute(
    insert(User).values(email=email).on_conflict_do_nothing(index_elements=["email"])
)
```

애플리케이션 코드의 중복 체크는 **UX**를 위한 것이고, 실제 보증은 **DB 제약**이 한다. 둘 다 필요하지만 역할이 다르다는 걸 알고 짜라. 제약 없이 코드 체크만 있으면 그건 보증이 아니다.

---

